extend type Query {
  categoryDetail(category_id: String!): Category!
  categoryList(parent_id: String! = ""): [Category!]!
}

extend type Mutation {
  categoryAdd(data: CategoryAdd!): Category!
  categoryUpdate(data: CategoryUpdate!): Category!
  categoryDelete(data: CategoryDelete!): Category!
}
